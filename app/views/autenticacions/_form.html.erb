<%= form_for(@autenticacion) do |f| %>
  <% if @autenticacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autenticacion.errors.count, "error") %> prohibited this autenticacion from being saved:</h2>

      <ul>
      <% @autenticacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
  <div class="row">
    <div class="box">
      <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center">
          Logeo de usuario
        </h2>
        <p>

        <div class="field">
          <%= f.label :user %><br>
          <%= f.text_field :user %>
        </div>
        <div class="field">
          <%= f.label :crypted_password %><br>
          <%= f.text_field :crypted_password %>
        </div>
        <div class="field">
          <%= f.label :email %><br>
          <%= f.text_field :email %>
        </div>
        <div class="actions">
          <%= f.submit %>
        </div>
        </p>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>
